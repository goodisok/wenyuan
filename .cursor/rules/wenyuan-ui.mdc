---
description: 问元前端 UI — 国风深色产品化规范（Jinja2 + 原生 JS + theme.css）
globs:
  - templates/**
  - static/css/**
  - static/js/app.js
alwaysApply: false
---

# 问元 UI 规则

## 技术约束

- **栈**：Jinja2 模板 + 单文件 `static/css/theme.css` + `static/js/app.js`（无 React/Vue/Tailwind）
- **禁止**：引入新 UI 框架；在模板里写 inline style；硬编码 `#hex`（须用 CSS 变量）
- **HTML 转义**：JS 渲染用户/服务端字符串必须用 `escapeHtml()`

## 设计令牌（必须引用）

见 `docs/DESIGN.md`。核心变量：

- 背景：`--bg` / `--bg-card` / `--bg-elevated`
- 文字：`--text` / `--text-muted`
- 强调：`--accent` / `--accent-dim` / `--accent-cool` / `--accent-warm`
- 五行：`--wood` `--fire` `--earth` `--metal` `--water`

## 组件类名（复用，勿发明平行体系）

| 用途 | 类名 |
|------|------|
| 页面卡片 | `.card` `.panel-card` |
| 主按钮 | `.btn.btn-primary` |
| 命盘分区 | `.section-block` `.chart-nav` |
| AI 区 | `.ai-empty-state` `.l2-chip` |
| 空/错/加载 | `.alert-error` `.chart-skeleton` `.spinner` |

## 页面 IA

### 首页 `/`

Hero → 排盘表单 → 能力亮点 → 最近排盘（localStorage）

### 命盘 `/chart`

1. 顶栏：返回 + 截图/PDF
2. Sticky：四柱摘要 + Tab
3. **基本** → **命盘** → **细盘** → **问 AI**
4. 规则层不在浏览器展示程序化面板（v1.14+）；解读走 AI Tab

## 文案调性

- 产品化、直接：「开始排盘」「开始解读」
- 避免：「仅供娱乐」「算命平台」口吻
- AI 区：锚定本盘、非闲聊；history 服务端上限 100 轮

## 反模式

- 不要恢复浏览器端观命/断事/三关面板（除非同步扩展 `public_insight` API）
- 不要拆出第二个 CSS 文件（除非用户明确要求）
- 不要在 `renderChart` 外重复绑定同 id 事件
